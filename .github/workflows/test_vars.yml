name: Variable Testing

on:
  workflow_dispatch:
  
jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - name: Variables
      run: echo ${{ github.server_url }}/${{ github.repository }}.git#${{ github.ref }}
